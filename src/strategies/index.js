const EMAStrategy = require('./ema.strategy');
const PullbackStrategy = require('./pullback.strategy');
const RangeStrategy = require('./range.strategy');
const BreakoutStrategy = require('./breakout.strategy');

module.exports = {
  EMAStrategy,
  PullbackStrategy,
  RangeStrategy,
  BreakoutStrategy
}; 